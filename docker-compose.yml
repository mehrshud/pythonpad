# Add a service for real-time collaboration
version: '3'
services:
  pythonpad:
    build: .
    environment:
      - COLLABORATION=True
    ports:
      - "8080:8080"
    depends_on:
      - redis
  redis:
    image: redis
    ports:
      - "6379:6379"
